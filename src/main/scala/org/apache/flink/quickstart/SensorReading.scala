package org.apache.flink.quickstart

case class SensorReading(
                        id: String,
                        timestamp: Long,
                        temperature: Double
                        )
